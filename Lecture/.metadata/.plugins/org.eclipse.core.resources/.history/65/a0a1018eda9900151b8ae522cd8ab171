var cities = ["London", "Paris", "Egypt", "Bangkok", "Uganda"];

//CommonJS Method for making variables available to other files
//First reference is how you will call the variable in other files
//Second reference is the name of the local variable
exports.cities = cities;

//Foods array is available because it is in the bootstrap file
console.log(foods[1]);

