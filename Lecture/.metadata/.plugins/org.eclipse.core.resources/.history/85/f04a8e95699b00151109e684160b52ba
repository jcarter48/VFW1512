var win = Ti.UI.createWindow({
	backgroundColor: "#fff",
	top: 20
});

for(var i=0; i<5; i++){
	
	var theView = Ti.UI.createView({
		width:100,
		height: 100,
		backgroundColor: "#C65E53",
		top: i*110,
		horizontal: i*110
	});
	win.add(theView);
}

win.open();
