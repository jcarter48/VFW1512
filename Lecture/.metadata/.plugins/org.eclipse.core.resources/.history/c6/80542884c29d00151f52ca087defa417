var win = Ti.UI.createWindow();

var view1 = Ti.UI.createView({ backgroundColor:'#66C247' });
var view2 = Ti.UI.createView({ backgroundColor:'#55424A9' });
var view3 = Ti.UI.createView({ backgroundColor:'#BD2E2E' });

var swipeView = Ti.UI.createScrollableView({
  views:[view1,view2,view3],
  showPagingControl:true,
  currentPage: 2
});

swipeView.addEventListener("scrollend", function(){
	console.log(this.currentPage);
	console.log(this.views[this.currentPage].backgroundColor);
});

win.add(swipeView);
win.open();